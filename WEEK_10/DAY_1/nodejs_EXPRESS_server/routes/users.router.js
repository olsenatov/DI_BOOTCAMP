const express = require('express');
const { getAllUsers, getUser } = require('../controllers/users.controller.js')

// u_route.get('/', getAllUsers);
// u_route.get('/', getUser);

// module.exports = { u_router };